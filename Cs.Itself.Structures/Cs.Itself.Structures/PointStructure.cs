using System;
using System.Collections.Generic;
using System.Text;

namespace Cs.Itself.Structures
{
    struct PointStructure
    {
        public uint x, y;

        public PointStructure (uint x, uint y)
        {
            this.x = x;
            this.y = y;
        }
    }
}
